# ── Settings + admin locks for CHANGEME (PUBLIC config-as-code) ──
# Mirror the admin locks below in manifest.json -> source_checks (committed) and
# runtime_forbidden (live). The audit workflow enforces source_checks on every push.

# ═══════════════ ADMIN LOCKS — do not change ═══════════════
# SOME_ADMIN_PASSWORD=
# SOME_ADMIN_TOGGLE=false
# ═══════════════════════════════════════════════════════════

# ─────────────── Gameplay settings (change via pull request) ───────────────
