# ── Compose substitution vars (PUBLIC — no secrets) ──
# Pin the image by DIGEST (the audit workflow fails if it isn't @sha256):
#   docker buildx imagetools inspect owner/image:tag
GAME_IMAGE=owner/image@sha256:REPLACE_WITH_REAL_DIGEST
SAVE_DIR=./saves
TZ=UTC
GAME_PORT=0000
